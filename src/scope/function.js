function greeting() {
    let userName = 'daniel';
    console.log(userName);

    if (userName === 'daniel') {   // === significa valor y tipo 
        console.log(`Hello ${userName}!`)  // funcion que declara una varibale y la asigna
    }
}

greeting();


